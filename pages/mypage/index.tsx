import React from 'react';
import { Layout, UnderConstruction } from '@components';

const MyPage = () => {
  return (
    <Layout title={`MyPage | FansSum::팬심이 모여 문화가 되다`}>
      <UnderConstruction description={`MyPage가 들어갈 예정입니다.`} />
    </Layout>
  );
};

export default MyPage;
