import { Layout } from '@layouts';
import { SearchPageContainer } from '@containers';

const SearchPage = () => {
  return (
    <Layout>
      <main>
        <SearchPageContainer />
      </main>
    </Layout>
  );
};

export default SearchPage;
